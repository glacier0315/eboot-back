<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.glacier.sys.dao.UserDao">

    <resultMap id="userMap" type="User">
        <id column="id" property="id" javaType="java.lang.String"/>
        <collection property="roles" select="com.glacier.sys.dao.RoleDao.findRolesByUserId"
                    column="{userId:id}" fetchType="lazy"></collection>
    </resultMap>

    <sql id="user_columns">
        a.id, a.username, a.password, a.salt, a.id_card, a.birthday, a.sex, a.status, a.email, a.mobile, a.dept_id, a.create_by, a.create_date
    </sql>

    <select id="findById" resultType="User">
        SELECT
        <include refid="user_columns"/>
        FROM t_user a
        where a.id=#{id}
    </select>

    <select id="findList" resultType="User">
        SELECT
        <include refid="user_columns"/>
        FROM t_user a
        <where>
            a.del_flag=#{NORMAL_FLAG}
            <if test="sex != null and sex != '' ">
                and a.sex=#{sex}
            </if>
            <if test="status != null and status != '' ">
                and a.status=#{status}
            </if>
            <if test="idCard != null and idCard != '' ">
                and a.id_card=#{idCard}
            </if>
            <if test="username != null and username != '' ">
                and a.username=#{username}
            </if>
        </where>
    </select>

    <select id="loadUserByUsername" resultMap="userMap">
        SELECT
        <include refid="user_columns"/>
        FROM t_user a
        <where>
            a.del_flag='0'
            and a.username=#{username}
        </where>
    </select>

    <insert id="insert" parameterType="User">
        INSERT INTO t_user
        (id, username, password, nickname, id_card, birthday, sex, status, salt, email, mobile, dept_id,
        create_by, create_date, del_flag)
        VALUES (#{id}, #{username}, #{password}, #{nickname}, #{idCard}, #{birthday}, #{sex}, #{status}, #{salt}, #{email}, #{mobile}, #{deptId},
        #{createBy}, #{createDate}, #{delFlag})
    </insert>

    <update id="update" parameterType="User">
        update t_user
        set id_card = #{idCard},
            birthday = #{birthday},
            sex = #{sex},
            status = #{status},
            nickname = #{nickname},
            email = #{email},
            mobile = #{mobile},
            dept_id = #{deptId},
            update_by = #{updateBy},
            update_date = #{updateDate}
        where id = #{id}
    </update>

    <update id="updatePassword" parameterType="User">
        update t_user
        set password = #{password},
            update_by = #{updateBy},
            update_date = #{updateDate}
        where id = #{id}
    </update>

    <update id="delete">
        update t_user a
        set a.del_flag=#{DEL_FLAG}
        where id=#{id}
    </update>
</mapper>