<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.glacier.sys.dao.RoleDao">

    <sql id="role_columns">
        a.id, a.role_name, a.role_code, a.status, a.create_by, a.create_date, a.update_by, a.update_date,
        a.del_flag
    </sql>

    <sql id="roleJoins">
        inner join t_user_role b on b.del_flag='0' and a.id=b.role_id
    </sql>

    <select id="get" resultType="Role">
        select
        <include refid="role_columns"/>
        from t_role a
        where id = #{id}
    </select>

    <select id="findList" resultType="Role">
        select
        <include refid="role_columns"/>
        from t_role a
        <where>
            a.del_flag=#{NORMAL_FLAG}
            <if test="roleName != null and roleName != '' ">
                and a.role_name=#{roleName}
            </if>
        </where>
    </select>

    <select id="findListByUser" resultType="Role">
        select
        <include refid="role_columns"/>
        from t_role a
        <include refid="roleJoins"/>
        <where>
            a.del_flag='0'
            and b.del_flag='0'
            and b.user_id = #{userId}
        </where>
    </select>

    <insert id="insert" parameterType="Role">
        insert into t_role (id, role_name, role_code,
            status, create_by, create_date,
            update_by, update_date, del_flag
          )
        values (#{id}, #{roleName}, #{roleCode},
            #{status}, #{createBy}, #{createDate},
            #{updateBy}, #{updateDate}, #{delFlag}
          )
    </insert>

    <update id="update" parameterType="Role">
        update t_role
        set role_name = #{roleName},
            role_code = #{roleCode},
            status = #{status},
            update_by = #{updateBy},
            update_date = #{updateDate}
        where id = #{id}
    </update>

    <update id="delete">
        update t_role a
        set a.del_flag=#{DEL_FLAG}
        where id=#{id}
    </update>
</mapper>