<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.glacier.sys.dao.UserRoleDao">

    <sql id="user_role_columns">
        a.id, a.role_id, a.user_id, a.create_by, a.create_date, a.update_by, a.update_date
    </sql>

    <select id="findById" resultType="UserRole">
        select
        <include refid="user_role_columns"/>
        from t_user_role a
        where id = #{id}
    </select>

    <select id="findList" resultType="UserRole">
        select
        <include refid="user_role_columns"/>
        from t_user_role a
        <where>
            a.del_flag=#{NORMAL_FLAG}
            <if test="roleId != null and roleId != '' ">
                and a.role_id=#{roleId}
            </if>
            <if test="userId != null and userId != '' ">
                and a.user_id=#{userId}
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="UserRole">
        insert into t_user_role (id, user_id, role_id,
            create_by, create_date,
            update_by, update_date, del_flag
          )
        values (#{id}, #{userId}, #{roleId},
            #{createBy}, #{createDate},
            #{updateBy}, #{updateDate}, #{delFlag}
          )
    </insert>

    <update id="update" parameterType="UserRole">
        update t_user_role
        set user_id = #{userId},
            role_id = #{roleId},
            update_by = #{updateBy},
            update_date = #{updateDate}
        where id = #{id}
    </update>

    <update id="delete">
        update t_user_role a
        set a.del_flag=#{DEL_FLAG}
        where id=#{id}
    </update>
</mapper>