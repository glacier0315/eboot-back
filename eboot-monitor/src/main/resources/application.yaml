spring:
  profiles:
    active: dev
  application:
    name: eboot-monitor
  cloud:
    consul:
      host: 192.168.10.128
      port: 8500
      discovery:
        # 注册到consul的服务名称
        serviceName: ${spring.application.name}


server:
  port: 8091



