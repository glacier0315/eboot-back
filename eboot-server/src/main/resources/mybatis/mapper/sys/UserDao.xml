<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.glacier.sys.dao.UserDao" >

    <sql id="user_columns">
        a.id, a.username, a.password, a.id_card as idCard, a.birthday, a.sex, a.status
    </sql>

    <insert id="insert" parameterType="User">
        INSERT INTO t_user
        (id, username, password, id_card, birthday, sex, status, create_by, create_date, del_flag)
        VALUES (#{id}, #{username}, #{password}, #{id_card}, #{birthday}, #{sex}, #{status}, #{create_by}, #{create_date}, #{del_flag})
    </insert>

    <select id="findList" resultType="User">
        SELECT
        <include refid="user_columns"/>
        FROM t_user a
        <where>
            a.del_flag=#{NORMAL_FLAG}
            <if test="username != null and username != '' ">
                and a.username=#{username}
            </if>
        </where>
    </select>

    <update id="delete">
        update t_user a
        set a.del_flag=#{DEL_FLAG}
        where id=#{id}
    </update>
</mapper>